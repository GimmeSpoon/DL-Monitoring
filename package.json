{
  "name": "server-monitor",
  "version": "2.0.0",
  "description": "Server monitoring: GPU + CPU/memory/disk over SSH, with history, event log and GPU usage log",
  "main": "server.js",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "start": "node server.js",
    "set-web-password": "node scripts/set-web-password.js",
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.0",
    "express-session": "^1.18.0",
    "node-ssh": "^13.2.0"
  }
}
